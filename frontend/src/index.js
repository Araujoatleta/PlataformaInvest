import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
  // Se você estiver usando um arquivo CSS global

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);